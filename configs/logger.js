const config = {
    folder: ".logs",
    filename: {
        HTTPLog: "request-all.log",
        ProcessLog: "process-all.log",
        DBLog: "database-all.log",
    },
};

module.exports = config;
